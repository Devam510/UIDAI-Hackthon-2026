# UIDAI Hackathon Environment Variables

# Groq API Configuration
# Get your free API key from: https://console.groq.com (no credit card required)
GROQ_API_KEY=your_api_key_here

# Instructions:
# 1. Go to https://console.groq.com
# 2. Sign up (free, no credit card)
# 3. Click "API Keys" in sidebar
# 4. Click "Create API Key"
# 5. Copy the key and replace "your_api_key_here" above
# 6. Save this file

# Database Configuration
# For local development: SQLite is used automatically (no DATABASE_URL needed)
# For production (Render): PostgreSQL URL is set automatically by Render
# DATABASE_URL=postgresql://user:password@host:port/database
# Note: Render provides this as postgres:// but backend converts to postgresql://

# Add other environment variables below as needed
