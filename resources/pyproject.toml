[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "uidai-trends-platform"
version = "0.1.0"
description = "UIDAI Trends Platform"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["backend"]
